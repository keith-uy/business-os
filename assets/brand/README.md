# assets/brand/

Put the logo, color values, and font names here so every client-facing deliverable uses the same brand. A short `brand.md` with hex codes and font names is enough for the agent to style HTML or PDF output consistently.
