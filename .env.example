# Copy to .env.local (gitignored). Root-level keys are for tools that serve the
# whole business. Keys that belong to ONE engagement go in that engagement's own
# .env.local (clients/<slug>/.env.local), never here.
#
# Examples:
# STRIPE_API_KEY=
# OPENAI_API_KEY=
